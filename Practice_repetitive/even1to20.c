// Write a C program that uses a for loop to print all even numbers from 1 to 20.
#include<stdio.h>

int main() {
    for (int i = 2; i <= 20; i += 2) {
        printf("%i\n", i);
    }
    return 0;
}
